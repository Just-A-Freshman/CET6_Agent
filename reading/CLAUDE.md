## 限制
你有新建任意文件并修改你新建文件的权利，但你不能对根目录下其他原有的文件进行任意修改，只具备已读权限。

## 输出
所有代码文件放置到 ./code 文件夹下面；输出的数据文件放置到 ./知识库/ 下；

## 外部AI
你可以编写代码调用deepseek api，api_key需要通过`.env`中的`DEEPSEEK_API_KEY`变量进行访问。

## 其他
1. Python解释器的路径："D:\Program_Files\Anaconda\python.exe"
2. 一些模型的存储路径，你对里面的模型，拥有只读权限，但绝对不允许修改："D:\Model"
